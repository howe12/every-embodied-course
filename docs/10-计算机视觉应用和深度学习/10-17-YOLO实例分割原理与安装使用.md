# 10-17 YOLO实例分割原理与安装使用

> **前置课程**：10-14 深度学习与神经网络、10-6 YOLO综合应用
> **后续课程**：待补充

**作者**：霍海杰 | **联系方式**：howe12@126.com

---

> **前置说明**：在前面的课程中，我们学习了目标检测（YOLO）和语义分割两大视觉任务。目标检测用矩形框标定"是什么"和"在哪里"，语义分割对每个像素进行类别标注。但你有没有遇到过这种情况：画面里有三只猫，语义分割只能告诉你"这些都是猫"，却分不清哪只是哪只；目标检测能框出三只猫的位置，但框内的背景也被当作猫了。**实例分割**（Instance Segmentation）正是解决这个问题的关键技术——它既要区分不同个体，还要精确勾勒出每个物体的轮廓。本节我们将深入学习实例分割与语义分割的区别、YOLO系列实例分割模型的发展脉络、YOLACT原理解析，以及如何在ROS2机器人系统中部署YOLO分割模型。准备好了吗？让我们开始吧！

---

## 目录

1. [实例分割概述](#1-实例分割概述)
2. [YOLO实例分割发展脉络](#2-yolo实例分割发展脉络)
3. [YOLO实例分割原理](#3-yolo实例分割原理)
4. [YOLO分割模型安装与使用](#4-yolo分割模型安装与使用)
5. [PyTorch实践代码](#5-pytorch实践代码)
6. [ROS2 YOLO分割节点](#6-ros2-yolo分割节点)
7. [练习题](#练习题)
8. [答案](#答案)

---

## 1. 实例分割概述

### 1.1 三种分割任务的区别

在计算机视觉中，"分割"是一个大家族，成员包括**语义分割**（Semantic Segmentation）、**实例分割**（Instance Segmentation）和**全景分割**（Panoptic Segmentation）。它们虽然都涉及对图像进行像素级分类，但解决问题的角度截然不同。

**语义分割**：给每个像素分配一个类别标签。所有"猫"都被标记为"猫"这个类别，不管画面里有几只猫。语义分割关注的是"这片区域是什么类别"，而不区分同类物体的不同个体。

**实例分割**：在语义分割的基础上，进一步区分同类物体的不同个体。同样是"猫"，实例分割会告诉你"这是猫A、那是猫B、另一只是猫C"，每个个体都有独立的掩膜（Mask）。实例分割的核心挑战是：**不仅要正确分类，还要正确计数和区分个体**。

**全景分割**：由FAIR于2019年提出，试图统一前两者。全景分割将场景中的所有内容分为"东西"（Things，可数物体，有明确边界）和"物质"（Stuff，无定形背景，如天空、墙面、道路）。全景分割为每个像素同时给出语义类别和实例ID，真正实现了"看得完整又分得清楚"。

```
原图：街道上有两辆汽车（红车、蓝车）和一个行人

语义分割输出：
[ 道路 道路 道路 道路 道路 道路 道路 道路 道路 ]
[ 道路  汽车  汽车  汽车 道路  行人  行人 道路 ]
[ 道路  汽车  汽车  汽车 道路  行人  行人 道路 ]
→ 所有像素只标注类别（汽车/行人/道路），同类物体不区分个体

实例分割输出：
[ 道路 道路 道路 道路 道路 道路 道路 道路 道路 ]
[ 道路 [红车] [红车] [红车] 道路 [行人] [行人] 道路 ]
[ 道路 [红车] [红车] [红车] 道路 [行人] [行人] 道路 ]
→ 每个物体有独立ID（红车#1，蓝车#2，行人#3），边界精确到像素

全景分割输出：
[ 道路 道路 道路 道路 道路 道路 道路 道路 道路 ]
[ 道路 [红车#1] [红车#1] [红车#1] 道路 [行人#2] [行人#2] 道路 ]
[ 道路 [红车#1] [红车#1] [红车#1] 道路 [行人#2] [行人#2] 道路 ]
→ 同时给出语义（车/行人/道路）和实例（#1/#2）， stuff 和 things 统一处理
```

**三种分割任务的对比总结：**

| 特性 | 语义分割 | 实例分割 | 全景分割 |
|------|---------|---------|---------|
| 分类粒度 | 类别级别 | 实例级别 | 类别+实例双级别 |
| 区分同类别个体 | ❌ 不区分 | ✅ 区分 | ✅ 区分 |
| 输出 | 每像素类别ID | 每像素(类别ID, 实例ID) | 每像素(类别ID, 实例ID) |
| 背景处理 | 标注为背景类 | 通常不分割背景 | stuff类统一处理 |
| 典型算法 | FCN, U-Net, DeepLab | Mask R-CNN, YOLACT, YOLOv8-seg | Panoptic FPN, UPSNet |
| 典型应用 | 自动驾驶道路分割、医学影像 | 机器人抓取、自动驾驶感知 | 完整场景理解 |

### 1.2 实例分割的应用场景

**（1）机器人抓取与操作**

机器人要从桌面抓取特定物体时，实例分割能精确勾勒出每个物体的形状轮廓，生成适合机械爪抓取的抓取点（grasp pose）。语义分割无法区分"哪个是我的目标物体"，目标检测的矩形框包含了太多背景像素。

```python
instance_seg_results = {
    "objects": [
        {
            "class": "bottle",
            "instance_id": 1,
            "mask": [[0,0,0,...], ...],
            "bbox": [150, 100, 200, 280],
            "centroid": (175, 190),
            "grasp_points": [
                {"position": (175, 150), "angle": 90, "width": 40},
                {"position": (175, 230), "angle": 90, "width": 35},
            ]
        },
        {
            "class": "cup",
            "instance_id": 2,
            "mask": [[0,0,0,...], ...],
            "bbox": [300, 120, 370, 260],
            "centroid": (335, 190),
            "grasp_points": [
                {"position": (335, 200), "angle": 0, "width": 50},
            ]
        }
    ]
}

print(f"目标物体：{instance_seg_results['objects'][0]['class']} "
      f"#{instance_seg_results['objects'][0]['instance_id']}")
print(f"抓取位置：{instance_seg_results['objects'][0]['grasp_points'][0]['position']}")
print(f"抓取角度：{instance_seg_results['objects'][0]['grasp_points'][0]['angle']}°")
```

**（2）自动驾驶感知**

在自动驾驶中，实例分割可以帮助车辆精确感知周围车辆、行人、骑行者等道路参与者。相比目标检测的矩形框，实例分割的像素级轮廓能更准确地判断本车与目标的空间关系，尤其在近距离 Cut-in 和近距离跟车场景中优势明显。

**（3）医学影像分析**

在细胞、病灶的计数和分析中，实例分割能区分重叠或相邻的细胞，分别计算它们的大小、面积等参数。这在病理切片分析、血细胞计数等场景中是不可替代的技术。

---

## 2. YOLO实例分割发展脉络

### 2.1 从目标检测到实例分割

YOLO系列最初是为目标检测任务设计的（YOLOv1-v5）。随着分割需求的增长，研究者们逐步将实例分割能力引入YOLO架构。

**YOLO实例分割发展时间线：**

| 年份 | 模型 | 关键创新 | 特点 |
|------|------|---------|------|
| 2019 | YOLACT | 提出原型掩膜+掩膜系数的两阶段方法 | 实时实例分割的开山之作 |
| 2020 | YOLACT++ | 改进Fast NMS、加入mask rescoring | 精度提升，延迟略有增加 |
| 2021 | YOLOPAF | 探索PANet和FPN的融合 | — |
| 2023 | YOLOv8-seg | YOLOv8内置分割头 | YOLO家族原生分割支持 |
| 2024+ | YOLO11-seg | 进一步优化 | 最新Ultralytics版本 |

### 2.2 YOLACT 原理详解

**YOLACT**（You Only Look At CoefficienTs）是第一个真正意义上的实时实例分割算法，由Daniel Boli等人在2019年CVPR发表。它的核心思想是**将实例分割解耦为两个并行的子任务**，从而实现高速推理。

**第一阶段：原型掩膜生成**

YOLACT使用一个**全卷积网络**（FCN）为整张图像生成一组**原型掩膜**（Prototype Masks）。这些原型掩膜是图像级别的高层特征表示，每张原型掩膜编码了图像的某方面信息。

```python
# YOLACT 原型掩膜生成示意
# 输入：图像特征图 (H x W)
# 输出：k 张原型掩膜 (k x H x W)

num_prototypes = 32

print(f"原型掩膜数量: {num_prototypes}")
print(f"每张原型掩膜尺寸: H x W (与特征图相同)")
print(f"原型掩膜是'通用积木'，本身不针对特定实例")
```

**第二阶段：检测头预测掩膜系数**

YOLACT的检测头为每个检测到的物体预测一组**掩膜系数**（Mask Coefficients）。每个系数是一个标量（可正可负），表示该物体"需要多少比例的某个原型掩膜"。

```python
# 掩膜系数预测示意
# 每个检测到的物体（边界框）会预测一个系数向量

# 对于某个检测到的物体（person，置信度0.92）
mask_coefficients = [0.5, -0.3, 0.8, 0.1, -0.6, ...]  # 长度为 num_prototypes=32

# 原型掩膜 (num_prototypes x H x W)
prototype_masks = ...  # 由第一阶段网络生成

# 最终掩膜 = sigmoid(系数向量 dot 原型掩膜)
```

**第三阶段：掩膜合成**

得到原型掩膜和掩膜系数后，最终的实例掩膜通过两者的线性组合生成：

$$
\text{Mask}_j = \sigma \left( \sum_{i=1}^{k} c_{j,i} \cdot P_i \right)
$$

其中 $P_i$ 是第 $i$ 张原型掩膜，$c_{j,i}$ 是第 $j$ 个实例在第 $i$ 个原型上的系数，$\sigma$ 是 sigmoid 函数。

```python
import numpy as np

def yolact_mask_synthesis(prototype_masks, mask_coefficients):
    """
    YOLACT 掩膜合成
    参数:
        prototype_masks: 原型掩膜，shape = (k, H, W)
        mask_coefficients: 掩膜系数，shape = (k,)
    返回:
        final_mask: 合成后的实例掩膜，shape = (H, W)
    """
    k = prototype_masks.shape[0]
    combined_mask = np.zeros_like(prototype_masks[0])
    for i in range(k):
        combined_mask += mask_coefficients[i] * prototype_masks[i]
    final_mask = 1 / (1 + np.exp(-combined_mask))
    return final_mask

# 示例
H, W = 138, 138
num_prototypes = 32
np.random.seed(42)
prototype_masks = np.random.randn(num_prototypes, H, W) * 0.5
mask_coefficients = np.random.randn(num_prototypes) * 1.5
final_mask = yolact_mask_synthesis(prototype_masks, mask_coefficients)
print(f"原型掩膜形状: {prototype_masks.shape}")
print(f"掩膜系数长度: {len(mask_coefficients)}")
print(f"合成掩膜形状: {final_mask.shape}")
print(f"掩膜值范围: [{final_mask.min():.4f}, {final_mask.max():.4f}]")
binary_mask = final_mask > 0.5
print(f"二值掩膜中 True 像素占比: {binary_mask.sum() / binary_mask.size:.2%}")
```

### 2.3 YOLO 系列分割模型对比

| 特性 | YOLACT | YOLACT++ | YOLOv8-seg |
|------|--------|---------|------------|
| 基础架构 | ResNet-50/101 | ResNet-50/101 + 可变形卷积 | C2f (YOLOv8) |
| 原型掩膜数量 | 32 | 32 | 内置于分割头 |
| NMS方式 | 标准NMS | Fast NMS | YOLOv8内置 |
| 掩膜分辨率 | 138×138 | 138×138 | 与输入图像相关 |
| 推理速度（1080p） | ~30 FPS | ~25 FPS | ~50+ FPS |
| mAP（COCO） | 29.8 | 31.2 | 37+ |

---

## 3. YOLO实例分割原理

### 3.1 YOLOv8-seg 的分割头设计

YOLOv8是Ultralytics公司于2023年发布的YOLO最新主干版本，它在目标检测的基础上增加了**分割头**（Segmentation Head），实现了一个模型同时完成检测和分割两个任务。

YOLOv8-seg 的分割原理与YOLACT有所不同。YOLOv8-seg 为每个检测到的物体直接预测一个二进制掩膜，而不是通过原型掩膜组合。这种"原生分割"方式简化了流程，提高了效率。

```python
# YOLOv8-seg 模型输出结构（简化）
model_output = {
    # 1. 检测头输出：边界框回归 + 类别分类
    "bboxes": {
        "shape": (batch, num_anchors, 84),  # 84 = 4(bbox) + 80(classes in COCO)
    },
    # 2. 分割头输出：原型掩膜 + 掩膜系数
    "masks": {
        "proto": (batch, 32, H, W),       # 原型掩膜（32张，HxW分辨率）
        "coeffs": (batch, num_anchors, 32)  # 每个anchor的掩膜系数
    }
}
# 最终掩膜 = sigmoid(proto dot coeffs) -> 裁剪到 bbox 区域 -> 二值化
```

### 3.2 非极大值抑制（NMS）在实例分割中的应用

在目标检测中，NMS用于去除同一个物体上的重叠检测框。在实例分割中，NMS同样重要，但操作对象从边界框变成了**掩膜**。

**为什么分割也需要NMS？**

目标检测网络中，模型会对同一个物体产生多个检测候选。如果不做NMS处理，同一个物体会出现多个检测框和多个分割掩膜，造成重复计数。

```python
import numpy as np

def mask_iou(mask1, mask2):
    """
    计算两个二值掩膜的 IoU（Intersection over Union）
    参数:
        mask1, mask2: 二值掩膜，shape = (H, W)，值为 True/False 或 0/1
    返回:
        iou: 掩膜 IoU 值，范围 [0, 1]
    """
    m1 = mask1.astype(np.int32)
    m2 = mask2.astype(np.int32)
    intersection = np.logical_and(m1, m2).sum()
    union = np.logical_or(m1, m2).sum()
    if union == 0:
        return 0.0
    return intersection / union


def mask_nms(masks, scores, iou_threshold=0.5):
    """
    基于掩膜 IoU 的非极大值抑制（NMS）
    参数:
        masks: 实例掩膜列表，每个元素是 shape=(H,W) 的二值掩膜
        scores: 对应每个掩膜的置信度分数
        iou_threshold: IoU 阈值，超过此值则认为两个掩膜重叠严重
    返回:
        keep_indices: 保留的掩膜索引
    """
    if len(masks) == 0:
        return []
    order = np.argsort(scores)[::-1]
    keep = []
    while len(order) > 0:
        i = order[0]
        keep.append(i)
        if len(order) == 1:
            break
        current_mask = masks[i]
        remaining_indices = order[1:]
        ious = np.array([mask_iou(current_mask, masks[idx]) for idx in remaining_indices])
        order = order[1:][ious < iou_threshold]
    return keep


print("=" * 50)
print("掩膜 NMS 示例")
print("=" * 50)
np.random.seed(123)
masks = []
scores = []
for i in range(5):
    mask = np.zeros((50, 50), dtype=bool)
    center_y, center_x = np.random.randint(10, 40), np.random.randint(10, 40)
    radius = np.random.randint(5, 15)
    y, x = np.ogrid[:50, :50]
    circle_mask = (y - center_y)**2 + (x - center_x)**2 <= radius**2
    masks.append(circle_mask)
    scores.append(np.random.uniform(0.5, 0.99))
# 添加一个与第0个掩膜高度重叠的掩膜（模拟重复检测）
masks.append(masks[0].copy())
scores.append(scores[0] - 0.05)
print(f"输入掩膜数量: {len(masks)}")
print(f"置信度分数: {[f'{s:.3f}' for s in scores]}")
keep_indices = mask_nms(masks, scores, iou_threshold=0.5)
print(f"NMS 保留的掩膜数量: {len(keep_indices)}")
print(f"保留的掩膜索引: {keep_indices}")
print("注：最后一个掩膜（重复检测）被正确过滤掉了")
```

### 3.3 掩膜生成与后处理

掩膜生成后，通常还需要经过一系列后处理步骤才能得到最终的分割结果：

```python
def mask_postprocessing(mask, bbox, image_shape):
    """
    掩膜后处理流程
    参数:
        mask: 原始掩膜，shape = (mask_h, mask_w)，值范围 [0, 1]
        bbox: 边界框 [x1, y1, x2, y2]
        image_shape: 原始图像尺寸 (H, W)
    返回:
        binary_mask: 处理后的二值掩膜，shape = (H, W)
    """
    import cv2
    H, W = image_shape
    x1, y1, x2, y2 = map(int, bbox)
    bbox_w = x2 - x1
    bbox_h = y2 - y1
    mask_resized = cv2.resize(mask, (bbox_w, bbox_h), interpolation=cv2.INTER_LINEAR)
    final_mask = np.zeros((H, W), dtype=np.float32)
    final_mask[y1:y2, x1:x2] = mask_resized
    binary_mask = final_mask > 0.5
    return binary_mask


def apply_mask_to_image(image, mask, color=(0, 255, 0), alpha=0.5):
    """
    将分割掩膜叠加到原始图像上
    参数:
        image: 原始图像，shape = (H, W, 3)，BGR格式
        mask: 二值掩膜，shape = (H, W)，bool类型
        color: 掩膜颜色 (B, G, R)
        alpha: 叠加透明度
    返回:
        result: 叠加后的图像
    """
    import cv2
    colored_mask = np.zeros_like(image)
    colored_mask[mask] = color
    result = cv2.addWeighted(image, 1, colored_mask, alpha, 0)
    return result
```

---

## 4. YOLO分割模型安装与使用

### 4.1 Ultralytics 库安装

Ultralytics 是 YOLO 官方维护团队提供的 Python 库，提供了从训练到部署的全套工具。通过 pip 安装即可使用：

```bash
pip install ultralytics
python -c "import ultralytics; print(ultralytics.__version__)"
```

### 4.2 预训练模型下载与使用

YOLOv8-seg 提供了多个预训练模型，从轻量到高精度排列如下：

| 模型 | 参数量 | mAP (COCO) | 推理速度 (FP32) |
|------|--------|-----------|----------------|
| yolov8n-seg.pt | 3.4M | 36.5 | ~1.5ms |
| yolov8s-seg.pt | 11.8M | 40.4 | ~2.5ms |
| yolov8m-seg.pt | 27.3M | 44.7 | ~4.5ms |
| yolov8l-seg.pt | 49.0M | 48.0 | ~7.5ms |
| yolov8x-seg.pt | 71.8M | 50.1 | ~12ms |

> **n/s/m/l/x**：nano/small/medium/large/xlarge，模型规模从小到大。

```python
from ultralytics import YOLO
import torch

# 检查环境
print(f"PyTorch 版本: {torch.__version__}")
print(f"CUDA 可用: {torch.cuda.is_available()}")
if torch.cuda.is_available():
    print(f"CUDA 版本: {torch.version.cuda}")
    print(f"GPU 型号: {torch.cuda.get_device_name(0)}")

# 加载预训练分割模型（会自动从官方下载模型权重）
model = YOLO('yolov8n-seg.pt')  # nano 模型，速度最快，适合嵌入式/机器人
print(f"模型加载成功！")
print(f"模型路径: {model.model_path}")
```

### 4.3 Python API 调用（推理与参数配置）

```python
from ultralytics import YOLO
import cv2
import numpy as np

# 加载模型
model = YOLO('yolov8n-seg.pt')

# =============================================
# 方法1：图像文件推理
# =============================================
def inference_image():
    """
    对单张图像进行实例分割推理
    """
    # 推理（可以传入图像路径、numpy数组、或URL）
    results = model('https://ultralytics.com/images/bus.jpg')
    result = results[0]
    num_instances = len(result.boxes)
    print(f"检测到 {num_instances} 个实例")
    if result.masks is not None:
        masks = result.masks.data.cpu().numpy()
        print(f"分割掩膜数量: {len(masks)}")
        print(f"每个掩膜形状: {masks[0].shape}")
    names = result.names
    print(f"类别列表: {list(names.values())}")
    return result

# =============================================
# 方法2：摄像头实时推理
# =============================================
def inference_camera(camera_index=0):
    """
    打开摄像头进行实时实例分割
    """
    cap = cv2.VideoCapture(camera_index)
    if not cap.isOpened():
        print(f"无法打开摄像头 {camera_index}")
        return
    print("按 'q' 键退出，按 's' 键保存当前帧")
    while True:
        ret, frame = cap.read()
        if not ret:
            print("无法读取帧")
            break
        results = model(frame, stream=True)
        for result in results:
            annotated_frame = result.plot()
            cv2.imshow('YOLOv8 Segmentation', annotated_frame)
        key = cv2.waitKey(1) & 0xFF
        if key == ord('q'):
            break
        elif key == ord('s'):
            cv2.imwrite('snapshot.jpg', frame)
            print("已保存当前帧到 snapshot.jpg")
    cap.release()
    cv2.destroyAllWindows()

# =============================================
# 方法3：视频文件推理
# =============================================
def inference_video(video_path='input.mp4', output_path='output.mp4'):
    """
    对视频文件进行实例分割
    """
    cap = cv2.VideoCapture(video_path)
    fps = int(cap.get(cv2.CAP_PROP_FPS))
    width = int(cap.get(cv2.CAP_PROP_FRAME_WIDTH))
    height = int(cap.get(cv2.CAP_PROP_FRAME_HEIGHT))
    total_frames = int(cap.get(cv2.CAP_PROP_FRAME_COUNT))
    print(f"视频信息: {width}x{height}, {fps}fps, 共{total_frames}帧")
    fourcc = cv2.VideoWriter_fourcc(*'mp4v')
    out = cv2.VideoWriter(output_path, fourcc, fps, (width, height))
    frame_count = 0
    while True:
        ret, frame = cap.read()
        if not ret:
            break
        results = model(frame, stream=True)
        for result in results:
            annotated_frame = result.plot()
            out.write(annotated_frame)
        frame_count += 1
        if frame_count % 30 == 0:
            print(f"处理进度: {frame_count}/{total_frames} ({100*frame_count/total_frames:.1f}%)")
    cap.release()
    out.release()
    print(f"处理完成，输出保存到: {output_path}")
```

### 4.4 推理参数详解

```python
def inference_with_params():
    """
    展示 YOLOv8 推理的各种参数配置
    """
    model = YOLO('yolov8n-seg.pt')

    # ===== 基础参数 =====
    # conf: 置信度阈值（默认 0.25），低于此值的检测结果被过滤
    # iou:  NMS 的 IoU 阈值（默认 0.7），用于去除重叠检测
    # max_det: 每张图像最多检测的实例数量（默认 300）

    results = model(
        'image.jpg',
        conf=0.5,      # 只保留置信度 >= 0.5 的结果（更严格）
        iou=0.45,      # NMS IoU 阈值（更激进地去重叠）
        max_det=50,    # 最多保留 50 个实例
        verbose=False, # 关闭推理过程日志输出
    )

    # ===== 分割专用参数 =====
    # retina_masks: True = 返回高分辨率掩膜（与原图分辨率相同）
    result = model('image.jpg', retina_masks=True)[0]
    if result.masks is not None:
        print(f"掩膜形状: {result.masks.data.shape}")  # (N, H, W)

    # ===== 设备选择 =====
    # device='cpu': 使用 CPU 推理
    # device='0':   使用第一块 GPU
    # device='0,1': 使用多块 GPU（数据并行）
    # device='mps': 使用 Apple Silicon GPU（Mac M系列）
    if torch.cuda.is_available():
        results_gpu = model('image.jpg', device='0')[0]
        print("使用 GPU 推理")
    else:
        results_cpu = model('image.jpg', device='cpu')[0]
        print("使用 CPU 推理")
    return results
```

---

## 5. PyTorch实践代码

### 5.1 YOLOv8-seg 图像实例分割

```python
"""
YOLOv8-seg 图像实例分割完整示例
"""

from ultralytics import YOLO
import cv2
import numpy as np
from pathlib import Path


def segment_image(image_path, model_path='yolov8n-seg.pt', save_dir='output'):
    """
    对图像进行实例分割并可视化结果
    参数:
        image_path: 输入图像路径
        model_path: 模型权重路径
        save_dir: 输出目录
    返回:
        result: 推理结果对象
        annotated_image: 带标注的图像
    """
    # 加载模型
    model = YOLO(model_path)
    # 创建输出目录
    Path(save_dir).mkdir(parents=True, exist_ok=True)
    # 读取图像
    image = cv2.imread(image_path)
    if image is None:
        raise ValueError(f"无法读取图像: {image_path}")
    print(f"输入图像尺寸: {image.shape[:2]}")
    # 执行推理
    results = model(image, conf=0.5, iou=0.45, retina_masks=True)
    result = results[0]
    # 获取分割结果
    if result.masks is not None:
        num_instances = len(result.masks.data)
        print(f"\n检测到 {num_instances} 个实例:")
        for i, (mask, box, cls) in enumerate(zip(
            result.masks.data.cpu().numpy(),
            result.boxes.xyxy.cpu().numpy(),
            result.boxes.cls.cpu().numpy()
        )):
            class_name = result.names[int(cls)]
            confidence = result.boxes.conf[i].item()
            mask_area = mask.sum()
            x1, y1, x2, y2 = box
            bbox_area = (x2 - x1) * (y2 - y1)
            fill_ratio = mask_area / bbox_area if bbox_area > 0 else 0
            print(f"  [{i+1}] {class_name}: 置信度={confidence:.2f}, "
                  f"掩膜面积={mask_area:.0f}px, 填充率={fill_ratio:.2%}")
    # 绘制结果并保存
    annotated_image = result.plot()
    output_path = f"{save_dir}/segmentation_result.jpg"
    cv2.imwrite(output_path, annotated_image)
    print(f"\n结果已保存到: {output_path}")
    return result, annotated_image
```

### 5.2 自定义数据集训练流程

在机器人应用中，通常需要针对特定场景训练自定义分割模型。

**Step 1：准备数据集（YOLO分割格式）**

```
dataset/
├── images/
│   ├── train/
│   │   ├── image1.jpg
│   │   └── image2.jpg
│   └── val/
│       └── image3.jpg
└── labels/
    ├── train/
    │   ├── image1.txt
    │   └── image2.txt
    └── val/
        └── image3.txt

# 分割标注格式（每行一个实例）：
# class_id polygon_point_1_x polygon_point_1_y polygon_point_2_x polygon_point_2_y ...
```

**Step 2：创建数据集配置文件**

```yaml
# dataset.yaml
path: /path/to/dataset
train: images/train
val: images/val
nc: 3
names:
  0: robot_arm
  1: target_object
  2: obstacle
```

**Step 3：启动训练**

```python
from ultralytics import YOLO
import torch

def train_custom_segmentation():
    model = YOLO('yolov8n-seg.pt')  # 加载预训练模型（迁移学习）
    results = model.train(
        data='dataset.yaml',
        epochs=100,
        imgsz=640,
        batch=16,
        device=0 if torch.cuda.is_available() else 'cpu',
        masks=32,                      # 原型掩膜数量
        optimizer='SGD',
        lr0=0.01,
        lrf=0.01,
        momentum=0.937,
        weight_decay=0.0005,
        augmentation=True,
        flipud=0.0,
        fliplr=0.5,
        mosaic=1.0,
        project='runs/segment',
        name='exp1',
        exist_ok=True,
        verbose=True,
    )
    print("训练完成！")
    print(f"最佳模型路径: {results.save_dir}/weights/best.pt")
    return results
```

**Step 4：评估与验证**

```python
def evaluate_model(model_path='runs/segment/exp1/weights/best.pt'):
    model = YOLO(model_path)
    metrics = model.val(
        data='dataset.yaml',
        iou=0.6,
        conf=0.5,
    )
    print("=" * 50)
    print("分割模型评估结果")
    print("=" * 50)
    print(f"Box mAP50: {metrics.box.map50:.4f}")
    print(f"Box mAP50-95: {metrics.box.map:.4f}")
    print(f"Seg mAP50: {metrics.seg.map50:.4f}")
    print(f"Seg mAP50-95: {metrics.seg.map:.4f}")
    return metrics
```

### 5.3 结果可视化（绘制分割掩膜）

```python
def visualize_masks(image, result):
    """
    自定义绘制分割掩膜（使用不同颜色）
    """
    import cv2

    # 为不同类别预设颜色（BGR格式）
    color_map = {
        'person': (100, 150, 255),
        'car': (130, 90, 50),
        'truck': (80, 130, 50),
        'bus': (50, 100, 180),
        'bicycle': (0, 255, 127),
        'dog': (80, 200, 200),
        'cat': (200, 100, 150),
    }

    output = image.copy()
    H, W = image.shape[:2]

    if result.masks is None:
        return output

    # 为每个实例绘制掩膜
    for i, (mask, box, cls) in enumerate(zip(
        result.masks.data.cpu().numpy(),
        result.boxes.xyxy.cpu().numpy(),
        result.boxes.cls.cpu().numpy()
    )):
        class_name = result.names[int(cls)]
        confidence = result.boxes.conf[i].item()

        # 获取该类别的颜色
        color = color_map.get(class_name, (i * 30 % 255, 100, 150))

        # 将掩膜 resize 到原图尺寸并叠加
        mask_resized = cv2.resize(mask, (W, H), interpolation=cv2.INTER_LINEAR)
        colored_mask = np.zeros_like(image)
        colored_mask[mask_resized > 0.5] = color
        output = cv2.addWeighted(output, 1.0, colored_mask, 0.4, 0)

        # 绘制边界
        contours, _ = cv2.findContours(
            (mask_resized > 0.5).astype(np.uint8),
            cv2.RETR_EXTERNAL, cv2.RETR_TREE
        )
        cv2.drawContours(output, contours, -1, color, 2)

        # 绘制标签
        x1, y1, x2, y2 = map(int, box)
        label = f"{class_name} {confidence:.2f}"
        cv2.putText(output, label, (x1, y1 - 10),
                     cv2.FONT_HERSHEY_SIMPLEX, 0.6, color, 2)

    return output
```

---


## 6. ROS2 YOLO分割节点

### 6.1 图像订阅、模型推理、分割结果发布

下面是一个完整的ROS2 YOLO分割节点实现。它订阅相机图像话题，进行实例分割推理，并发布带掩膜标注的图像和分割结果。

```python
#!/usr/bin/env python3
# yolo_segmentation_node.py
# ROS2 YOLOv8 实例分割节点

import rclpy
from rclpy.node import Node
from sensor_msgs.msg import Image
from cv_bridge import CvBridge
import cv2
import numpy as np
from ultralytics import YOLO


class YOLOSegmentationNode(Node):
    def __init__(self):
        super().__init__('yolo_segmentation_node')
        self.declare_parameter('model_path', 'yolov8n-seg.pt')
        self.declare_parameter('confidence_threshold', 0.5)
        self.declare_parameter('iou_threshold', 0.45)
        self.declare_parameter('device', 'cpu')
        self.declare_parameter('input_topic', '/image_raw')
        self.declare_parameter('output_topic', '/segmentation/image')

        model_path = self.get_parameter('model_path').value
        device = self.get_parameter('device').value
        input_topic = self.get_parameter('input_topic').value
        output_topic = self.get_parameter('output_topic').value

        self.get_logger().info(f'YOLOv8 Segmentation Node 启动')
        self.get_logger().info(f'模型: {model_path}')
        self.get_logger().info(f'输入话题: {input_topic}')

        try:
            self.model = YOLO(model_path)
            self.get_logger().info('模型加载成功！')
        except Exception as e:
            self.get_logger().error(f'模型加载失败: {e}')
            raise

        self.bridge = CvBridge()
        self.subscription = self.create_subscription(
            Image, input_topic, self.image_callback, 10
        )
        self.publisher = self.create_publisher(
            Image, output_topic, 10
        )
        self.get_logger().info('节点初始化完成，等待图像输入...')

    def image_callback(self, msg):
        try:
            cv_image = self.bridge.imgmsg_to_cv2(msg, desired_encoding='bgr8')
            results = self.model(
                cv_image,
                conf=self.get_parameter('confidence_threshold').value,
                iou=self.get_parameter('iou_threshold').value,
                device=self.get_parameter('device').value,
                verbose=False
            )
            result = results[0]
            annotated_image = result.plot()
            output_msg = self.bridge.cv2_to_imgmsg(
                annotated_image, encoding='bgr8'
            )
            output_msg.header = msg.header
            self.publisher.publish(output_msg)

            if result.masks is not None:
                for i, (mask, box, cls) in enumerate(zip(
                    result.masks.data.cpu().numpy(),
                    result.boxes.xyxy.cpu().numpy(),
                    result.boxes.cls.cpu().numpy()
                )):
                    class_name = result.names[int(cls)]
                    confidence = result.boxes.conf[i].item()
                    mask_area = mask.sum()
                    self.get_logger().debug(
                        f'检测到 {class_name} '
                        f'(conf={confidence:.2f}, area={mask_area:.0f}px)'
                    )

        except Exception as e:
            self.get_logger().error(f'图像处理失败: {str(e)}')


def main(args=None):
    rclpy.init(args=args)
    node = YOLOSegmentationNode()
    try:
        rclpy.spin(node)
    except KeyboardInterrupt:
        pass
    finally:
        node.destroy_node()
        rclpy.shutdown()


if __name__ == '__main__':
    main()
```

### 6.2 掩膜应用与可视化

在机器人应用中，分割掩膜不仅用于可视化，更重要的是用于**掩膜抠图**和**空间计算**。

```python
def extract_object_from_mask(image, mask):
    """
    使用分割掩膜从原图中提取物体区域
    参数:
        image: 原始图像，shape = (H, W, 3)
        mask: 二值掩膜，shape = (H, W)，bool 类型
    返回:
        object_image: 仅包含该物体的图像区域（其他区域为黑）
        bbox: 物体的边界框 [x1, y1, x2, y2]
    """
    import cv2
    H, W = image.shape[:2]
    background = np.zeros_like(image)
    object_image = np.where(mask[:,:,np.newaxis], image, background)
    rows = np.any(mask, axis=1)
    cols = np.any(mask, axis=0)
    if rows.any() and cols.any():
        y1, y2 = np.where(rows)[0][[0, -1]]
        x1, x2 = np.where(cols)[0][[0, -1]]
        bbox = [x1, y1, x2, y2]
    else:
        bbox = [0, 0, W, H]
    return object_image, bbox


def compute_object_properties(image, mask):
    """
    计算分割物体的几何属性
    """
    import cv2
    props = {}
    props['mask_area'] = mask.sum()
    rows = np.any(mask, axis=1)
    cols = np.any(mask, axis=0)
    y1, y2 = np.where(rows)[0][[0, -1]]
    x1, x2 = np.where(cols)[0][[0, -1]]
    props['bbox'] = [x1, y1, x2, y2]
    props['bbox_width'] = x2 - x1
    props['bbox_height'] = y2 - y1
    props['bbox_area'] = props['bbox_width'] * props['bbox_height']
    props['fill_ratio'] = props['mask_area'] / props['bbox_area'] if props['bbox_area'] > 0 else 0
    moments = cv2.moments(mask.astype(np.uint8))
    if moments['m00'] > 0:
        props['centroid_x'] = moments['m10'] / moments['m00']
        props['centroid_y'] = moments['m01'] / moments['m00']
    else:
        props['centroid_x'] = (x1 + x2) / 2
        props['centroid_y'] = (y1 + y2) / 2
    masked_image = image.copy()
    masked_image[~mask] = 0
    props['mean_color'] = masked_image[mask].mean(axis=0) if mask.sum() > 0 else np.array([0, 0, 0])
    contours, _ = cv2.findContours(mask.astype(np.uint8), cv2.RETR_EXTERNAL, cv2.CHAIN_APPROX_SIMPLE)
    if contours:
        props['perimeter'] = cv2.arcLength(contours[0], True)
        props['contour_area'] = cv2.contourArea(contours[0])
    else:
        props['perimeter'] = 0
        props['contour_area'] = 0
    return props
```

---

## 本章小结

本章我们系统性地学习了YOLO实例分割的原理、安装和使用方法。

在实例分割概述部分，我们区分了语义分割、实例分割和全景分割三种分割任务。语义分割只标注类别，实例分割在此基础上区分同类物体的不同个体，全景分割则统一处理 things 和 stuff。实例分割在机器人抓取、自动驾驶感知、医学影像分析等领域有广泛应用。

在YOLO实例分割发展脉络部分，我们回顾了从YOLACT到YOLOv8-seg的演进历程。YOLACT开创性地提出原型掩膜+掩膜系数的两阶段方法，实现了实时实例分割；YOLOv8-seg则在内置分割头中集成了分割能力，成为当前最流行的YOLO分割方案。

在YOLO实例分割原理部分，我们深入剖析了YOLACT的原型掩膜合成机制和YOLOv8-seg的分割头设计。掩膜NMS是去除重复检测的关键步骤，掩膜后处理包括resize、裁剪和阈值化等操作。

在模型安装与使用部分，我们掌握了Ultralytics库的安装、预训练模型的使用方法，以及Python API调用（图像、视频、摄像头推理）的完整流程。

在PyTorch实践代码部分，我们通过具体示例学习了图像分割、视频分割、自定义数据集训练和结果可视化等实战技能。

在ROS2 YOLO分割节点部分，我们实现了订阅图像、模型推理、结果发布的完整pipeline，并掌握了掩膜应用（物体提取、几何属性计算、可视化）的方法。

---

## 练习题

**1. 选择题（单选）**

**题目1.1：** 以下哪种分割任务可以区分画面中三只不同的猫，并分别给出它们的精确轮廓？

A. 语义分割
B. 实例分割
C. 全景分割
D. 目标检测

**题目1.2：** YOLACT算法的核心思想是将实例分割解耦为哪两个并行子任务？

A. 特征提取 + 分类
B. 原型掩膜生成 + 掩膜系数预测
C. 边界框回归 + 掩膜预测
D. 语义分割 + 实例分割

**题目1.3：** YOLOv8-seg模型中，每个检测到的实例最终掩膜的生成方式是什么？

A. 直接从分割头输出二进制掩膜
B. 通过原型掩膜与掩膜系数的线性组合生成
C. 通过语义分割结果后处理得到
D. 通过模板匹配得到

**2. 填空题**

**题目2.1：** 在YOLACT中，用于对原型掩膜进行加权求和的系数称为 ______ （请填写术语）。

**题目2.2：** 当两个实例掩膜的 IoU 超过 NMS 阈值时，应保留置信度较 ______ （高/低）的那个，删除另一个。

**题目2.3：** Ultralytics 库中，用于将 ROS 图像消息和 OpenCV 图像格式进行互相转换的工具是 ______ （请填写Python模块名）。

**3. 简答题**

**题目3.1：** 请简要说明语义分割、实例分割和全景分割三种任务的核心区别，并各举一个机器人领域的应用场景。

**题目3.2：** 假设你需要为机器人开发一个"从桌面抓取指定物体"的功能。请说明实例分割相比目标检测，在这个任务中有什么独特优势？至少列出2点。

**4. 编程题**

**题目4.1：** 请编写一个Python函数 `compute_mask_centroid_and_area`，输入为一个二值掩膜（numpy数组，dtype=bool），输出为该掩膜的质心坐标（cx, cy）和像素面积。要求使用 OpenCV 的 `moments` 函数计算质心。

---

## 答案

### 1. 选择题答案

**题目1.1：** **B. 实例分割**

解析：实例分割的核心特性就是区分同类物体的不同个体。对于"三只猫"，实例分割会生成三个独立的掩膜，每个掩膜对应一只具体的猫。语义分割只能标注"这些都是猫"，无法区分个体；全景分割虽然也能区分个体，但它是语义分割和实例分割的统一；目标检测输出的是矩形框，不是精确轮廓。

**题目1.2：** **B. 原型掩膜生成 + 掩膜系数预测**

解析：YOLACT的核心创新在于将实例分割解耦为两个并行的子任务：第一阶段由全卷积网络生成一组原型掩膜（Prototype Masks），第二阶段由检测头为每个实例预测一组掩膜系数（Mask Coefficients），最终通过线性组合得到实例掩膜。

**题目1.3：** **B. 通过原型掩膜与掩膜系数的线性组合生成**

解析：YOLOv8-seg虽然使用了更简化的设计，但其分割头仍然包含原型掩膜和掩膜系数两个部分。推理时，通过 $Mask = \sigma(P \cdot C)$ 计算最终掩膜，其中 $P$ 是原型掩膜，$C$ 是掩膜系数。

### 2. 填空题答案

**题目2.1：** **掩膜系数**（Mask Coefficients）

解析：掩膜系数是YOLACT第二阶段为每个实例预测的系数向量，每个系数对应一张原型掩膜，用于控制该原型在最终掩膜中的贡献程度（可正可负）。

**题目2.2：** **高**（高）

解析：NMS（Non-Maximum Suppression）的目的是保留最优的检测结果。当两个检测（掩膜）高度重叠时，保留置信度较高的那个，删除置信度较低的重复检测。

**题目2.3：** **cv_bridge**

解析：`cv_bridge`是ROS2中用于在sensor_msgs/Image和OpenCV图像格式之间进行互相转换的标准工具。使用`bridge.imgmsg_to_cv2()`将ROS图像转为OpenCV图像，使用`bridge.cv2_to_imgmsg()`将OpenCV图像转为ROS消息。

### 3. 简答题答案

**题目3.1：**

三种分割任务的核心区别：

**语义分割**：为每个像素分配一个类别标签，不区分同类物体的不同个体。所有"车"都被标注为同一类别。
- 应用场景：自动驾驶中的道路、可行驶区域分割（机器人需要知道"哪里可以走"）

**实例分割**：在语义分割基础上，区分同类物体的不同个体，每个个体有独立的掩膜。
- 应用场景：机器人从桌面抓取物体（需要区分"哪个杯子是要抓的"）

**全景分割**：统一处理things（可数物体）和stuff（无定形背景），每个像素同时给出语义类别和实例ID。
- 应用场景：完整场景理解（服务机器人在复杂室内环境中需要知道"这是沙发#1，旁边是茶几#2，地上有地毯（stuff）"）

**题目3.2：**

实例分割在机器人抓取任务中的独特优势：

**优势1：精确物体轮廓**
目标检测的矩形框包含了太多背景像素和非物体区域。机械爪如果按照矩形框执行抓取，可能会夹到框内的背景或其他物体。实例分割的像素级掩膜能精确勾勒物体边界，生成更准确的抓取点，提高抓取成功率。

**优势2：区分重叠或相邻的同类物体**
当桌面上有多个同类物体时，目标检测只能框出它们的位置，但无法区分"哪个杯子是目标"。实例分割可以为每个杯子生成独立的掩膜和ID，机器人可以根据指令精准选择目标物体。

### 4. 编程题答案

```python
import cv2
import numpy as np


def compute_mask_centroid_and_area(mask):
    """
    计算二值掩膜的质心坐标和像素面积
    参数:
        mask: 二值掩膜，shape = (H, W)，dtype = bool
    返回:
        centroid: 质心坐标 (cx, cy)，单位为像素
        area: 掩膜面积（True 像素的数量）
    """
    area = int(mask.sum())
    M = cv2.moments(mask.astype(np.uint8))
    if M['m00'] > 0:
        cx = M['m10'] / M['m00']
        cy = M['m01'] / M['m00']
        centroid = (float(cx), float(cy))
    else:
        centroid = (0.0, 0.0)
    return centroid, area


def compute_mask_centroid_and_area_example():
    H, W = 100, 100
    mask = np.zeros((H, W), dtype=bool)
    center_y, center_x = 30, 30
    radius = 20
    y, x = np.ogrid[:H, :W]
    circle_mask = (y - center_y)**2 + (x - center_x)**2 <= radius**2
    mask |= circle_mask
    mask[60:80, 60:80] = True
    centroid, area = compute_mask_centroid_and_area(mask)
    print("掩膜质心与面积计算结果")
    print(f"掩膜形状: {mask.shape}")
    print(f"掩膜面积: {area} 像素")
    print(f"质心坐标: ({centroid[0]:.2f}, {centroid[1]:.2f}) 像素")
    # 验证
    circle_area = circle_mask.sum()
    rect_area_count = 20 * 20
    combined_area = circle_area + rect_area_count
    circle_centroid = (30.0, 30.0)
    rect_centroid = (70.0, 70.0)
    weighted_cx = (circle_centroid[0] * circle_area + rect_centroid[0] * rect_area_count) / combined_area
    weighted_cy = (circle_centroid[1] * circle_area + rect_centroid[1] * rect_area_count) / combined_area
    print(f"理论质心: ({weighted_cx:.2f}, {weighted_cy:.2f})")
    print(f"计算质心: ({centroid[0]:.2f}, {centroid[1]:.2f})")
    empty_mask = np.zeros((50, 50), dtype=bool)
    empty_centroid, empty_area = compute_mask_centroid_and_area(empty_mask)
    print(f"空掩膜测试: 质心={empty_centroid}, 面积={empty_area}")
    return centroid, area


if __name__ == '__main__':
    compute_mask_centroid_and_area_example()
```

**答案解析：**

本题考察的是OpenCV的`moments`函数在质心计算中的应用。`cv2.moments()`计算图像的几何矩，其中：
- `m00`：零阶矩，即图像的面积（白色像素的总和）
- `m10`：一阶矩X（x坐标加权和）
- `m01`：一阶矩Y（y坐标加权和）

质心计算公式：$c_x = m_{10}/m_{00}, \quad c_y = m_{01}/m_{00}$

当掩膜为空（`m00=0`）时，需要特殊处理避免除零错误。本题也说明了机器人视觉处理中的重要工程问题：**必须处理空掩膜、异常值等边界情况**。
